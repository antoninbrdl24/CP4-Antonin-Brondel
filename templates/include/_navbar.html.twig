<div class="gradient-navbar-ab">
	<div class="collapse px-4" id="navbarToggleExternalContent" data-bs-theme="dark">
		<div>
			<ul class="list-group list-group-flush">
				<a href="#">
					<li>Nos menus</li>
				</a>
				<a href="{{ path('app_contact') }}">
					<li>Contact</li>
				</a>
			</ul>
		</div>
	</div>

	<nav class="navbar">
		<div class="container-fluid container-navbar">
			<button class="navbar-toggler navbar-border" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggleExternalContent" aria-controls="navbarToggleExternalContent" aria-expanded="false" aria-label="Toggle navigation">
				<img class=" nav-link my-nav-icon" src="{{ asset('build/images/burger.svg') }}">
			</button>
			<a href="{{ path('app_index')}}">
				<img src="{{ asset('build/images/logo-wcs.svg') }}">
			</a>

			{% if app.user %}
			<div class="dropdown">
				<button class="btn btn-primary dropdown-toggle me-5" type="button" data-bs-toggle="dropdown" aria-expanded="false">
					{{ app.user.name}}
				</button>
				<ul class="dropdown-menu dropdown-menu-lg-end">
					<li><a class="dropdown-item" href="{{ path('app_my_profile')}}">Mes informations</a></li>
					{% if is_granted('ROLE_ADMIN') %}
					<li><a class="dropdown-item" href="#">Dashboard</a></li>
					{% endif %}
					<li><a class="dropdown-item" href="{{ path('app_logout')}}">Déconnexion</a></li>
				</ul>
			</div>
			
			{% else %}
				<a href="{{ path('app_login')}}">
					<img class="nav-link my-nav-icon" src="{{ asset('build/images/login.svg') }}">
				</a>
			{% endif %}

		</div>
	</nav>
</div>
