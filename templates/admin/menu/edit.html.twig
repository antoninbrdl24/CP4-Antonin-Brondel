{% extends 'dashboard.html.twig' %}

{% block title %}Modification de notre artiste{% endblock %}

{% block body %}
    <h1 class="mx-auto pt-4">Modification des intitulés du Menu</h1>
    <div class="container mx-auto border-general">
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.description) }}
        <button type="submit" class="btn btn-danger"> Sauvegarder ses changements</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_menu_index') }}" class="btn btn-primary mt-2">Retour</a>
     </div>
{% endblock %}